#include<iostream>
#include<math.h>
using namespace std;

int isPresent(int a[],int n,int x)
{
    for(int i=0;i<n;i++)
    {
        if(x==a[i])
        return 1;
    }
    return 0;
}

int isPrime(int num)
{
    if(num==0)
    return 0;
    
    if(num==1)
    return 0;
    
    for(int i=2;i<=sqrt(num);i++)
    {
        if(num%i==0)
        return 0;
    }
    return 1;
}

int main()
{
    int n,m;
    cin>>n>>m;
    
    int a[m-n+1];
    int j=0;
    for(int i=n;i<=m;i++)
    {
        if(isPrime(i)==1)
        {
            a[j]=i;
            j++;
        }
    }
    
    int c=0;
    int s;
    for(int k=0;k<j;k++)
    {
        s=isPresent(a,j,a[k]+6);
        if(s==1)
        {
            c++;
        }
    }
    if(c==0)
    {
        cout<<"No Prime Pairs";
    }
    else
    {
        cout<<c;
    }
    return 0;
}
